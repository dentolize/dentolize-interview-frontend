// import { gql, useQuery } from "@apollo/client";
// // import {  TNewCustomer } from "../types/api-types";

// export const GetAllCustomers = () => useQuery();
// export const GetOneCustomer = (id:string) => useQuery();


// // export const CreateNewCustomer =  ({firstName, lastName, email, phone}: TNewCustomer) => useQuery(CREATE_CUSTOMER);


// // export const DeleteCustomer = (id: string) => useMutation(REMOVE_CUSTOMER);